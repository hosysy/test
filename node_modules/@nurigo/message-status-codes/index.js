'use strict'

/**
 * @author hosy
 */

const list = require('./code.json')

/**
 * 넘어온 상태코드에 맞는 메시지를 리턴
 *
 * @param {String} code - 메시지 상태 코드
 */
module.exports.get = (code) => list[code] ? list[code] : null

/**
 * 상태코드 리스트를 전부 리턴
 *
 * @param {String} code - 메시지 상태 코드
 */
module.exports.getAll = () => list
